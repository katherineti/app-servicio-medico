export interface TokenAuth {
  [x: string]: any;
  id: number;
  // username: string;
  email: string;
  role: string;
  // iat?: number;
  // exp?: number;
  // idroles: number;
  // idcuenta: string;
  // moneda: string;
  // zonahoraria: string;
  // idsucursal: string;
  // accountType: boolean;
  // initialScreen?: boolean;
  // iva: number;
  // comision: number;
}
